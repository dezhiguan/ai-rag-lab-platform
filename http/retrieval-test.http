### 向量检索测试（V2 基线验证）
### 使用前：后端已启动；该 kbId 已导入文档并执行 POST /api/kb/{kbId}/embedding/rebuild
POST http://localhost:8080/api/retrieval/test
Content-Type: application/json

{
  "kbId": 1,
  "query": "短信验证码发不出去怎么排查？",
  "topK": 5
}
